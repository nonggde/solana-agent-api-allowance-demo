{
  "name": "solana-agent-api-allowance-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A TypeScript demo showing how Solana subscriptions and allowances can bound AI agent API spend.",
  "scripts": {
    "demo": "tsx src/demo.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
